// This empty assembly file is a placeholder to trick the Go compiler.
// It marks the package as "incomplete", which is required to allow
// the use of //go:linkname when compiled as a dependency in external projects.